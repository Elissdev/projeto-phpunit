# Projeto de Estudos PHPUnit 🚀

Repositório criado para estruturação e aprendizado de testes de unidade em backend utilizando PHPUnit.

## Como rodar o teste

Para executar a validação inicial, rode o comando abaixo na raiz do projeto:

`./vendor/bin/phpunit tests/ExemploTest.php`
